Copy this directory (`.env-example`) to `.env` and fill in the necessary environment variables.

Every developer has their own copy of the secret environment variables. The production server also has its own version of the secret environment variables.
